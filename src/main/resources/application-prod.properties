# Utiliser PostgreSQL pour le profil prod
spring.datasource.url=jdbc:postgresql://localhost:5432/formation_db
spring.datasource.username=postgres
spring.datasource.password=root
spring.datasource.driver-class-name=org.postgresql.Driver

# Assurez-vous que la base de donn�es est synchronis�e (par exemple, pour production, utilisez 'validate' ou 'none')
spring.jpa.hibernate.ddl-auto=create

# Afficher les requ�tes SQL g�n�r�es dans la console (d�sactiv� en production)
spring.jpa.show-sql=false
